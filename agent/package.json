{
  "name": "agent-workspace",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "check": "pnpm -r check",
    "supabase:start": "npx supabase start",
    "supabase:serve": "npx supabase functions serve orchestrator --env-file supabase/.env.local",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "supabase": "^2.98.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.5"
  }
}
