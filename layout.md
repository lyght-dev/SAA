# SAA 문제풀이 앱/웹뷰 레이아웃 및 유스케이스 명세

## 1. 문서 목적과 범위

- **MUST** 이 문서는 SAA 문제풀이 앱/웹뷰의 화면 구조, 화면 간 이동, 문제 풀이 상태 및 핵심 사용자 행동을 정의해야 한다.
- **MUST** 앱과 웹뷰는 동일한 정보 구조와 핵심 사용자 흐름을 제공해야 한다.
- **MUST NOT** 이 문서에 정의되지 않은 커뮤니티, 랭킹, 결제, 로그인 등의 기능을 필수 범위로 간주해서는 안 된다.
- **MUST NOT** 색상, 글꼴, 여백 등 별도의 디자인 시스템에서 결정할 표현 규칙을 이 문서에서 임의로 확정해서는 안 된다.

## 2. 용어 및 상태

- **MUST** `콘텐츠 도메인`은 문제를 구분하는 최상위 카테고리를 의미해야 한다.
- **MUST** `문제 분류`는 콘텐츠 도메인보다 세부적인 문제 메타데이터를 의미해야 한다.
- **MUST** 각 문제는 `안 푼 문제`, `맞춘 문제`, `틀린 문제` 중 정확히 하나의 현재 상태를 가져야 한다.
- **MUST** 최초 제출 전 문제의 상태는 `안 푼 문제`여야 한다.
- **MUST** 정답 제출 후 문제의 현재 상태를 `맞춘 문제`로 변경해야 한다.
- **MUST** 오답 제출 후 문제의 현재 상태를 `틀린 문제`로 변경해야 한다.
- **MUST** 같은 문제를 다시 제출하면 가장 최근 채점 결과로 현재 상태를 갱신해야 한다.
- **MUST NOT** 하나의 문제를 동시에 둘 이상의 현재 상태로 표시해서는 안 된다.
- **MUST NOT** `다시 풀기`를 누른 사실만으로 문제의 기존 상태를 변경해서는 안 된다.

## 3. 전체 정보 구조

- **MUST** 전체 화면 계층은 `인트로 → 홈 → 콘텐츠 도메인 목록 → 문제 목록 → 문제 풀이 → 해설` 순서를 지원해야 한다.
- **MUST** 홈에서 `틀린 문제 다시 풀기 → 틀린 문제 목록 → 문제 풀이 → 해설` 순서를 별도 진입 경로로 지원해야 한다.
- **MUST** 문제 목록과 틀린 문제 목록에서 선택한 문제의 문제 풀이 화면으로 이동할 수 있어야 한다.
- **MUST** 해설 화면에서 현재 문제를 다시 풀거나 다음 문제로 이동할 수 있어야 한다.
- **MUST** 인트로를 제외한 하위 화면의 헤더에서 홈 화면으로 이동할 수 있어야 한다.
- **MUST NOT** 사용자가 홈으로 돌아가기 위해 문제를 제출하도록 강제해서는 안 된다.

```text
[인트로]
    └─ [홈]
       ├─ 문제 풀러가기
       │  └─ [콘텐츠 도메인 목록]
       │     └─ [문제 목록: 1~N]
       │        └─ [문제 풀이]
       │           └─ Submit → [해설]
       │                         ├─ 다시 풀기 → [같은 문제 풀이]
       │                         └─ 다음 문제 풀기 → [다음 문제 풀이]
       └─ 틀린 문제 다시 풀기
          └─ [틀린 문제 목록]
             └─ [문제 풀이]
                └─ Submit → [해설]
```

## 4. 콘텐츠 도메인

- **MUST** 콘텐츠 도메인 목록에 `콘텐츠 도메인 1: 보안 아키텍처 설계`를 표시해야 한다.
- **MUST** 콘텐츠 도메인 목록에 `콘텐츠 도메인 2: 복원력을 갖춘 아키텍처 설계`를 표시해야 한다.
- **MUST** 콘텐츠 도메인 목록에 `콘텐츠 도메인 3: 고성능 아키텍처 설계`를 표시해야 한다.
- **MUST** 콘텐츠 도메인 목록에 `콘텐츠 도메인 4: 비용에 최적화된 아키텍처 설계`를 표시해야 한다.
- **MUST** 사용자가 콘텐츠 도메인을 선택하면 해당 도메인에 속한 문제만 문제 목록에 표시해야 한다.
- **MUST NOT** 콘텐츠 도메인 목록에 동일한 도메인을 중복 표시해서는 안 된다.
- **MUST NOT** 선택한 콘텐츠 도메인과 무관한 문제를 해당 문제 목록에 섞어서는 안 된다.

## 5. 화면별 레이아웃 구조

### 5.1 인트로 화면

- **MUST** 앱/웹뷰 진입 시 인트로 화면을 제공해야 한다.
- **MUST** 인트로가 끝나면 홈 화면으로 이동할 수 있어야 한다.
- **MUST NOT** 인트로 화면이 핵심 기능 진입을 영구적으로 막아서는 안 된다.

### 5.2 홈 화면

- **MUST** 홈 화면의 첫 번째 핵심 행동으로 `문제 풀러가기`를 제공해야 한다.
- **MUST** 홈 화면의 두 번째 핵심 행동으로 `틀린 문제 다시 풀기`를 제공해야 한다.
- **MUST** 두 행동은 사용자가 서로 다른 기능임을 인식할 수 있도록 독립된 선택 요소로 표시해야 한다.
- **MUST NOT** `문제 풀러가기` 선택 시 틀린 문제만으로 범위를 제한해서는 안 된다.
- **MUST NOT** `틀린 문제 다시 풀기` 선택 시 맞춘 문제 또는 안 푼 문제를 목록에 포함해서는 안 된다.

### 5.3 콘텐츠 도메인 목록 화면

- **MUST** 홈에서 `문제 풀러가기`를 선택했을 때 콘텐츠 도메인 목록 화면을 표시해야 한다.
- **MUST** 네 개의 콘텐츠 도메인을 각각 선택 가능한 목록 항목으로 표시해야 한다.
- **MUST** 선택한 콘텐츠 도메인의 문제 목록으로 이동해야 한다.
- **MUST NOT** 콘텐츠 도메인을 선택하기 전에 특정 문제 풀이 화면으로 임의 이동해서는 안 된다.

### 5.4 문제 목록 화면

- **MUST** 선택한 콘텐츠 도메인에 속한 문제를 `1`부터 `N`까지 번호 목록으로 표시해야 한다.
- **MUST** 각 목록 항목의 주 콘텐츠에는 문제 번호만 표시해야 한다.
- **MUST** `안 푼 문제`, `맞춘 문제`, `틀린 문제`를 목록에서 시각적으로 서로 구분해야 한다.
- **MUST** 상태 구분을 색상 외에도 아이콘, 테두리, 라벨 또는 형태 중 하나 이상으로 보완해야 한다.
- **MUST** 문제 번호를 선택하면 해당 문제의 문제 풀이 화면으로 이동해야 한다.
- **MUST** 문제 수 `N`은 선택한 콘텐츠 도메인에 실제로 포함된 문제 수를 기준으로 해야 한다.
- **MUST NOT** 문제 제목, 문제 본문 또는 정답을 목록 항목의 주 콘텐츠로 표시해서는 안 된다.
- **MUST NOT** 색상만으로 문제 상태를 구분해서는 안 된다.
- **MUST NOT** 한 도메인의 문제 번호 목록에 다른 도메인의 문제를 포함해서는 안 된다.

### 5.5 틀린 문제 목록 화면

- **MUST** 홈에서 `틀린 문제 다시 풀기`를 선택했을 때 현재 상태가 `틀린 문제`인 문제만 표시해야 한다.
- **MUST** 각 목록 항목의 주 콘텐츠에는 문제 번호만 표시해야 한다.
- **MUST** 문제 번호를 선택하면 해당 문제의 문제 풀이 화면으로 이동해야 한다.
- **MUST** 틀린 문제가 없을 때 빈 상태임을 명확히 안내해야 한다.
- **MUST NOT** 틀린 문제가 없을 때 비어 있는 목록만 아무 설명 없이 표시해서는 안 된다.
- **MUST NOT** 정답이나 해설을 목록에서 노출해서는 안 된다.

### 5.6 문제 풀이 화면

- **MUST** 화면을 위에서부터 `상단 정보 영역 → 문제 영역 → 보기 영역 → 제출 영역` 순으로 배치해야 한다.
- **MUST** 상단 정보 영역에 현재 문제의 콘텐츠 도메인을 표시해야 한다.
- **MUST** 상단 정보 영역에 현재 문제의 콘텐츠 도메인과 문제 분류 정보를 항상 표시해야 한다.
- **MUST** 상단 정보 영역 아래의 문제 영역에 문제 본문을 표시해야 한다.
- **MUST** 문제 영역 아래의 보기 영역에 선택 가능한 모든 보기를 표시해야 한다.
- **MUST** 사용자가 선택한 보기와 선택하지 않은 보기를 시각적으로 구분해야 한다.
- **MUST** 이미 제출한 문제에 다시 진입하면 가장 최근에 제출한 선택 답안을 선택된 상태로 복원해야 한다.
- **MUST** 문제 상태와 가장 최근 제출 답안을 함께 저장해야 한다.
- **MUST** 보기 영역 아래에 선택한 답을 제출하는 `Submit` 버튼을 배치해야 한다.
- **MUST** 유효한 보기가 선택된 경우에만 답안을 제출할 수 있어야 한다.
- **MUST** `Submit` 실행 시 선택한 답안을 채점하고 해설 화면으로 이동해야 한다.
- **MUST NOT** 보기 위에 `정답을 선택하세요`와 같은 별도 선택 안내 문구를 시각적으로 표시해서는 안 된다.
- **MUST NOT** 콘텐츠 도메인 또는 문제 분류를 켜고 끄는 토글을 표시해서는 안 된다.
- **MUST NOT** 제출 전에 정답, 채점 결과 또는 해설을 노출해서는 안 된다.
- **MUST NOT** 답안을 선택하지 않은 상태에서 빈 답안을 제출해서는 안 된다.
- **MUST NOT** 한 번의 제출 동작을 중복 처리해서는 안 된다.

### 5.7 해설 화면

- **MUST** 화면을 위에서부터 `채점 결과 영역 → 요구사항 분석 → AWS 서비스 분석 → 선택지 분석 → 후속 행동 영역` 순으로 배치해야 한다.
- **MUST** 채점 결과 영역을 문제 풀이 화면의 문제 영역과 유사한 상단 위치에 배치해야 한다.
- **MUST** 정답 제출 결과는 `Correct`로 표시해야 한다.
- **MUST** 오답 제출 결과는 `Incorrect`로 표시해야 한다.
- **MUST** `Correct`와 `Incorrect`는 텍스트를 포함해 시각적으로 명확히 구분해야 한다.
- **MUST** 채점 결과 아이콘은 투명한 배경 위에서 결과 그라데이션과 조화되는 색조로 표시해야 한다.
- **MUST** 채점 결과 배경의 그라데이션은 천천히 움직이는 은은한 애니메이션을 제공해야 한다.
- **MUST NOT** 채점 결과 아래에 격려 문구 또는 정답 안내 설명을 별도로 표시해서는 안 된다.
- **MUST** 첫 번째 해설 섹션으로 `요구사항 분석`을 표시해야 한다.
- **MUST** 두 번째 해설 섹션으로 `AWS 서비스 분석`을 표시해야 한다.
- **MUST** 세 번째 해설 섹션으로 `선택지 분석`을 표시해야 한다.
- **MUST** `선택지 분석`에서 전체 선택지 판단 근거와 각 선택지가 적절하거나 부적절한 이유를 식별할 수 있게 해야 한다.
- **MUST** 해설의 마지막 하단에 `다시 풀기`와 `다음 문제 풀기` 버튼을 제공해야 한다.
- **MUST** `다시 풀기`를 선택하면 현재 문제의 답안 선택을 초기화한 문제 풀이 화면으로 이동해야 한다.
- **MUST** `다음 문제 풀기`를 선택하면 현재 목록 순서의 다음 문제 풀이 화면으로 이동해야 한다.
- **MUST** 현재 문제가 목록의 마지막 문제라면 `다음 문제 풀기`로 존재하지 않는 문제에 이동해서는 안 되며, 완료 안내 또는 출발한 문제 목록으로의 복귀 행동을 제공해야 한다.
- **MUST NOT** 세 분석 섹션의 순서를 바꿔 표시해서는 안 된다.
- **MUST NOT** 채점 결과를 색상만으로 전달해서는 안 된다.
- **MUST NOT** `다시 풀기` 선택 시 직전 선택 답안을 선택된 상태로 유지해서는 안 된다.

## 6. 유스케이스

### UC-01. 앱 시작 및 홈 진입

- **MUST** 주 액터는 학습자여야 한다.
- **MUST** 학습자가 앱/웹뷰에 진입하면 인트로 화면을 표시해야 한다.
- **MUST** 인트로 이후 `문제 풀러가기`와 `틀린 문제 다시 풀기`가 있는 홈 화면을 표시해야 한다.
- **MUST NOT** 홈 진입만으로 문제 상태를 생성하거나 변경해서는 안 된다.

### UC-02. 콘텐츠 도메인에서 문제 선택

- **MUST** 사전 조건은 학습자가 홈 화면에 있는 것이어야 한다.
- **MUST** 학습자가 `문제 풀러가기`를 선택하면 네 개의 콘텐츠 도메인을 표시해야 한다.
- **MUST** 학습자가 콘텐츠 도메인을 선택하면 해당 도메인의 `1~N` 문제 번호 목록을 표시해야 한다.
- **MUST** 학습자가 문제 번호를 선택하면 해당 문제 풀이 화면을 표시해야 한다.
- **MUST** 해당 문제를 이전에 제출했다면 가장 최근 제출 답안을 선택된 상태로 표시해야 한다.
- **MUST NOT** 문제 번호를 선택하는 것만으로 해당 문제를 맞춘 문제 또는 틀린 문제로 기록해서는 안 된다.

### UC-03. 문제 답안 제출

- **MUST** 사전 조건은 학습자가 문제 풀이 화면에서 유효한 보기를 선택한 것이어야 한다.
- **MUST** 학습자가 `Submit`을 선택하면 선택 답안과 정답을 비교해야 한다.
- **MUST** 정답이면 문제 상태를 `맞춘 문제`로 저장하고 `Correct` 해설 화면을 표시해야 한다.
- **MUST** 오답이면 문제 상태를 `틀린 문제`로 저장하고 `Incorrect` 해설 화면을 표시해야 한다.
- **MUST** 채점 후 `요구사항 분석 → AWS 서비스 분석 → 선택지 분석` 순서로 해설을 제공해야 한다.
- **MUST NOT** 채점 완료 후 문제 상태 저장과 화면에 표시된 결과가 서로 다르게 처리되어서는 안 된다.

### UC-04. 같은 문제 다시 풀기

- **MUST** 사전 조건은 학습자가 해설 화면에 있는 것이어야 한다.
- **MUST** 학습자가 `다시 풀기`를 선택하면 같은 문제의 문제 풀이 화면으로 이동해야 한다.
- **MUST** 이전에 선택한 답안을 초기화해야 한다.
- **MUST** 학습자가 다시 제출하면 새로운 채점 결과로 문제의 현재 상태를 갱신해야 한다.
- **MUST NOT** 다시 제출하기 전에 기존 상태를 임의로 삭제하거나 변경해서는 안 된다.

### UC-05. 다음 문제 풀기

- **MUST** 사전 조건은 학습자가 해설 화면에 있고 현재 목록에 다음 문제가 존재하는 것이어야 한다.
- **MUST** 학습자가 `다음 문제 풀기`를 선택하면 현재 진입한 목록에서 현재 문제 다음 순서에 있는 문제의 풀이 화면으로 이동해야 한다.
- **MUST** 다음 문제에 저장된 제출 답안이 있으면 이를 복원하고, 없으면 답안 선택을 초기화한 상태로 표시해야 한다.
- **MUST NOT** 이전 문제의 선택 답안을 다음 문제에 전달해서는 안 된다.

### UC-06. 틀린 문제 다시 풀기

- **MUST** 사전 조건은 하나 이상의 문제가 현재 `틀린 문제` 상태인 것이어야 한다.
- **MUST** 학습자가 홈에서 `틀린 문제 다시 풀기`를 선택하면 틀린 문제 번호 목록을 표시해야 한다.
- **MUST** 학습자가 번호를 선택하면 해당 문제 풀이 화면으로 이동해야 한다.
- **MUST** 해당 문제에서 가장 최근에 제출한 오답을 선택된 상태로 복원해야 한다.
- **MUST** 재제출 결과가 정답이면 해당 문제를 `맞춘 문제`로 갱신하고 틀린 문제 목록에서 제외해야 한다.
- **MUST** 재제출 결과가 오답이면 해당 문제를 `틀린 문제`로 유지해야 한다.
- **MUST NOT** 현재 상태가 틀린 문제가 아닌 문제를 이 흐름의 목록에 표시해서는 안 된다.

### UC-07. 제출 답안 복원

- **MUST** 사전 조건은 학습자가 한 번 이상 제출한 문제를 다시 선택한 것이어야 한다.
- **MUST** 문제 풀이 화면에 가장 최근 제출 답안을 선택된 상태로 표시해야 한다.
- **MUST** 복원된 답안을 변경하고 다시 제출하면 새 답안과 채점 결과로 저장 상태를 갱신해야 한다.
- **MUST NOT** 저장된 답안이 없는 문제에 다른 문제의 선택 답안을 표시해서는 안 된다.

## 7. 공통 동작 및 접근성

- **MUST** 모든 선택 요소와 버튼은 터치와 키보드 입력으로 실행할 수 있어야 한다.
- **MUST** 현재 포커스, 선택 답안, 비활성 버튼 및 문제 상태를 서로 구분할 수 있어야 한다.
- **MUST** 화면 제목과 주요 행동의 읽기 순서가 시각적 배치 순서와 일치해야 한다.
- **MUST** 문제 본문과 보기가 작은 화면에서도 가로 스크롤 없이 읽히도록 재배치되어야 한다.
- **MUST** 로딩 중에는 제출과 화면 전환의 중복 실행을 방지해야 한다.
- **MUST** 데이터 로딩 실패 시 실패 사실과 다시 시도할 수 있는 행동을 제공해야 한다.
- **MUST NOT** 마우스 호버에만 의존하는 핵심 정보나 행동을 제공해서는 안 된다.
- **MUST NOT** 작은 화면에서 문제 본문, 보기 또는 핵심 버튼이 잘리거나 겹치게 해서는 안 된다.
- **MUST NOT** 오류 발생 시 사용자가 이미 선택한 답안을 불필요하게 초기화해서는 안 된다.

## 8. 범위 밖 또는 추후 결정 사항

- **MUST** 로그인 및 사용자 간 동기화 여부는 별도 제품 요구사항에서 결정해야 한다.
- **MUST** 인트로의 노출 시간, 건너뛰기 방식 및 재노출 정책은 별도 제품 요구사항에서 결정해야 한다.
- **MUST** 문제 상태의 저장 위치와 영속 기간은 별도 기술 명세에서 결정해야 한다.
- **MUST NOT** 위 미결정 사항을 구현 단계에서 근거 없이 임의 확정해서는 안 된다.
